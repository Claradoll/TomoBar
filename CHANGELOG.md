# 更新记录

## 1.6.0 · 2026-09-13

- 便签编号清单可转为任务组，按缩进保留分支；复用已有单任务关联和历史。
- 可折叠任务组、独立计时与时长设置、组和分支汇总、层级搜索；组按钮开始下一项待办。
- 便签右键直接专注当前清单项；任务名从正文同步，待办完成状态双向同步。
- 持久段落标识保证重复转换、编辑、缩进、撤销和重开不重复建任务；删行保留任务历史。
- 58 项核心测试与 126 项便签/WPF 检查通过。

## 1.5.6

- 便签拖出屏幕后，松手会完整移回显示器工作区，避免右侧裁切或被底部任务栏遮挡。
- 底部吸附的便签折叠后，标题条仍贴在底部；展开向上恢复，右侧贴边位置也保持。
- 超出工作区的窗口尺寸会收回可见范围，保留自由拖离边缘的行为。
- 新增四边越界恢复和反复折叠／展开保持右下贴边的回归检查。

## 1.5.5

- 修复便签吸附边缘后无法拖离的问题。
- 拖动过程中不再改写窗口候选位置，改为松手时吸附，避免连续小位移被反复抵消。
- 新增展开／折叠状态下从四边慢速拖离、松手贴边和非移动操作不误吸附的回归检查。

## 1.5.4

- 主界面便签标题支持单击编辑、Enter／失焦保存和 Esc 取消，同步显示在已打开便签的顶部。
- 顶部加入置顶按钮，移除最大化按钮，保留最小化和关闭；置顶与右键菜单同步。
- 双击标题栏折叠为标题小条，再双击恢复正文与原有高度。
- 拖动时吸附当前屏幕工作区边缘，阈值随显示缩放调整，展开时保持窗口在屏幕内。
- 修复便签标题编辑与主界面 Esc 快捷键、列表自动刷新之间的冲突。

## 1.5.3

- 移除独立便签窗口的标题输入区域，标题只在主界面便签列表显示。
- 移除「已保存／正在保存」底部提示，扩大正文空间。
- 保留已有标题、后台自动保存和关窗保存；保存失败时仍会提示并保留窗口。

## 1.5.2

- 精简便签右键菜单：文字、段落、列表分组，便签管理集中到「便签选项」，剪切／复制按选区启用。
- 待办勾选改为独立开关，支持与编号／分点列表组合，新增 Ctrl+1 快捷键。
- 多行段落操作保留选区，支持连续设置编号与待办；修复全选和选区结束于下一段开头时的处理。
- 组合列表支持回车续行、空项退出、缩进、插入重编号、撤销／重做与保存恢复。
- 文字位置计算排除自动编号，避免格式操作移动选区，以及空编号项不能正常退出。
- 保留旧便签兼容性，组合格式使用带版本保护的数据结构。

## 1.5.1

- 任务栏计时条双击直接打开主界面，保留单击开始／暂停与中键跳过休息。
- 修复任务栏右键菜单不能方便收起的问题；左键关闭菜单不会误触计时，切换应用时菜单收起。
- 我的任务支持单击预览时间；当前计时继续，点击开始才切换任务。
- 便签改为简洁纸张布局，移除常驻编辑工具栏，将格式、列表、颜色、置顶、链接与任务关联移入右键菜单。
- 优化便签浅色／深色纸色、紧凑窗口、菜单样式与编辑选区保留。

## 1.5.0

- 新增原生富文本便签：文字样式、标题、列表、待办勾选与网页链接。
- 支持自动保存、撤销／重做、独立窗口置顶、颜色标记、搜索与回收站恢复。
- 支持关联现有任务、便签转任务；标题为空时自动使用正文第一行。
- 新增主界面便签页、任务栏／托盘右键入口和可配置的全局快捷键。
- 便签以结构化数据存储，兼容旧版任务数据，并纳入备份恢复。
- 保留原有番茄钟计时、统计、自启及中键跳过休息的交互。

## 1.4.1

- 任务栏计时条支持中键单击跳过短／长休息，包括暂停中的休息。
- 悬停及可访问性描述增加“中键跳过休息”提示。
- 专注与停止时中键不触发，滚动滚轮不跳过；防止阶段切换、连续点击或延迟左键误操作。
- 跳过与小卡片行为一致：保留专注记录，回到待开始状态。

## 1.4.0

首次 GitHub 正式发布。

- 统一导航、任务操作和窗口按钮的矢量图标，改善中英文字体、数字字重及深浅色文字对比度。
- 保留红色番茄、圆角矩形、状态边框与圆点，以及任务栏单击计时、双击卡片的交互。
- 小屏幕专注卡片支持滚动，完整任务名可悬停查看。
- 修复从同一任务的休息阶段点击开始时误恢复休息的问题。
- 修复不同数据目录实例之间的唤醒／退出定位，以及托盘单双击互相干扰。
- 保存失败时不再显示保存成功；退出前无法保存时保留程序窗口。
- 保留开机自启、默认任务名、任务统计、备份恢复与 CSV 导出。
- 增加 Windows 自动构建、核心回归测试、Release 便携包与 SHA-256 校验。

## 1.3.0

开机自启开关、后台启动与独立的当前用户登记。

## 1.2.x

任务栏贴合可见性修复、快捷卡片、默认任务一键启动；迭代状态配色、圆角矩形、等宽数字和整体间距。

## 1.1.x

任务清单、任务时长统计、自动保存，以及缺失 UTC 日期的兼容修复。
