TomoBar · 小番茄 v1.5.6

- 便签拖出屏幕后，松手自动完整移回可用区域，右侧不会裁切，底部不会被任务栏遮住。
- 底部贴边后双击折叠，标题条继续贴在底部；展开时向上恢复正文。右侧贴边位置同样保留。
- 拖动过程中仍然自由移动，已贴边的便签可以正常拖离。

44 项核心测试和 104 项便签检查通过，共 148 项。覆盖展开／折叠状态下四边越界恢复、慢速拖离，以及反复折叠展开后的右下边缘对齐。

### 下载与升级

- `TomoBar-1.5.6-win-x64.zip`：解压后运行 `小番茄.exe`。
- `TomoBar.exe`：单文件版。
- `SHA256SUMS.txt`：文件校验值。

正常退出旧版，只替换程序，保留 `data` 文件夹。原有任务、统计和便签内容均保留。
