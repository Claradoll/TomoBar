# v1.5.1 验证记录

本地环境：Windows 11 x64，150% 显示缩放。2026-09-13。

35 项核心回归测试、70 项番茄钟桌面检查、35 项便签专项检查通过，共 140 项。

本轮新增覆盖：双击计时条打开主界面且不改变计时状态；左键关闭右键菜单且不触发延迟单击；应用失去激活时收起菜单；单击任务预览不改变当前计时、选中任务持久化字段和专注记录；显式开始时保存上一任务并切换；暂停与停止状态下预览／开始；便签移除工具栏、右键菜单与子菜单打开、格式保留选区、格式撤销。

便签覆盖：旧数据迁移、格式序列化、标题自动生成、链接协议限制、重复标识验证、自动保存与关窗保存、格式重开恢复、待办点击、完成样式与原格式分离、勾选撤销／重做、列表快捷输入与续行、编号缩进、标题样式切换、置顶、颜色选择、右键菜单打开、搜索、回收站恢复、转任务去重、备份还原和退出前最后输入。

已检查便签浅色／深色、380×440 编辑窗口、280×240 小窗口、便签列表，以及主界面、设置、任务栏和小卡片。截图使用测试样例，不包含个人内容。为了避免两个计时条互相遮挡，番茄钟屏幕命中回归在旧版正常退出后执行。

## 范围

测试在隔离数据目录中运行，操作真实 WPF 控件事件、编辑命令、Windows 鼠标与激活消息并等待消息循环；包含实际控件启用状态、渲染与 WindowFromPoint 检查。不是完整人工输入法或鼠标硬件测试。没有真实重启、锁屏、睡眠或多屏拓扑切换。

中文文本与备份格式已验证；复杂输入法组合、超长便签和外部软件富文本粘贴未做兼容性矩阵。粘贴有意采用纯文本，不导入任意 XAML、脚本或附件。便签正文由白名单结构化字段构造原生控件。

真实用户数据更新前备份，源码和安装包排除所有任务数据。GitHub Windows CI 执行核心回归与构建，桌面专项需在有 Explorer 的 Windows 会话中执行。
