# v1.6.0 验证记录

Windows 11 x64，150% 缩放，2026-09-13。58 项核心测试、126 项便签/WPF 检查全部通过，共 184 项。

新增核心覆盖：编号层级、旧关联任务升级且计时保持、同名条目和跨便签独立身份、重复转换、时长与历史保留、空白过滤、删除/撤销、调整缩进/排序、显式新增、双向勾选与当前计时保存、组汇总不重复计入全局、下一项选择、单轮完成后仍进入休息、备份恢复与旧段落 ID 迁移。

新增 WPF 覆盖：右键转换和直接开始、组预览、折叠、嵌套任务按钮、编号续行保留身份、撤销、自动保存联动、外部完成刷新正文、双向撤销、开始后主界面收起、预览不切换计时、改标题、层级搜索、关闭重开及再次转换。使用真实控件事件与编辑命令；测试执行 Enter 前显式激活编辑器。

已渲染并核对浅色/深色任务组与便签「番茄专注」子菜单。测试样例不包含用户数据。本轮保留并通过之前的 44 项核心和 104 项便签检查，原任务栏屏幕回归未重复执行。

## 既有检查与范围


本地环境：Windows 11 x64，150% 显示缩放。2026-09-13。

v1.5.6 当轮 44 项核心测试、104 项便签专项检查通过，共 148 项。

新增覆盖：主界面单击标题编辑、后台正文保存不丢失标题输入、Enter 保存并同步顶部标题、Esc 取消、失焦保存、留空回退正文标题；置顶按钮与右键同步；原生标题栏双击折叠和展开、保持宽度与展开高度、折叠时置顶和关闭、关闭前输入保存、重新打开恢复标题和置顶、最小化与恢复；系统最大化命令抑制；原生移动消息保持候选位置，退出移动循环后执行工作区边缘吸附。

吸附几何检查涵盖四边、远离阈值、负坐标副屏与 150% 阈值。负坐标与不同缩放为合成矩形验证，未进行真实多显示器拖动或拓扑切换。

保留并通过原有便签富文本、组合编号待办、选区保留、列表续行、缩进、撤销／重做、自动保存、备份恢复、回收站与转任务检查。

已渲染检查浅色／深色便签、新顶部按钮、38 像素折叠标题条、便签列表和紧凑窗口。截图为隔离测试样例，不包含个人内容。

v1.5.5 修复验证（v1.5.6 当轮继续通过）：在真实便签窗口上发送进入移动、连续移动和退出移动消息。每次从当前窗口位置增加 3 个像素的小步位移，再使用窗口处理后的矩形更新位置，共 20 步；展开／折叠状态分别覆盖四边，最终均能拖离 60 个像素。另验证松手靠边才吸附、吸附不改变尺寸，以及只进入／退出尺寸调整循环不会沿用前次移动状态。上一版只检查单次候选矩形，未覆盖这种反馈循环，现已补上。

v1.5.6 当轮新增：展开／折叠便签分别越过左、右、上、下边缘后，通过退出移动循环恢复完整可见区域；右下贴边连续三次折叠／展开，确认标题条及展开窗口底边不变、不被任务栏覆盖；尺寸、正文保持不变。几何检查另覆盖负坐标显示器、超大窗口缩回工作区和普通悬浮便签不跳至底部。

## 范围

桌面检查操作真实 WPF 控件事件、编辑命令和原生窗口消息，等待布局与消息循环；不是完整人工键鼠、中文输入法或多屏兼容性矩阵。保存失败时沿用提示与取消关窗行为。

v1.5.6 当轮未重复任务栏屏幕检查。历史 v1.5.2 的额外任务栏回归受全屏前台窗口影响，未计入通过数。

真实用户数据升级前备份，公开源码和安装包排除任务与便签数据。GitHub Windows CI 执行核心回归与构建；便签桌面专项在本机隔离目录执行。
